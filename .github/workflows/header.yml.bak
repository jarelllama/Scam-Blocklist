name: Update blocklist header and README.md count
run-name: Update domains count
on:
  push:
    paths:
      - "domains"
jobs:
  Update-count:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Update count
        run: bash "workflow_scripts/header.sh"
