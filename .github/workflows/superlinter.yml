name: Super-Linter
run-name: Super-Linter
on:
  workflow_dispatch:
  push:

jobs:
  super-linter:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Super-linter
        uses: super-linter/super-linter@v6.3.0
        env:
          #GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}