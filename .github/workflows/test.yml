name: Test workflow
on:
  workflow_dispatch:
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "${{vars.git_username}}"
          echo "${{secrets.git_email}}"
