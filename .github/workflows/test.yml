name: Test workflow
on:
  workflow_dispatch:
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "${{var.git_name}}"
          echo "${{secrets.git_email}}"
