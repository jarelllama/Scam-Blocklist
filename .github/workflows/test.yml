name: Test workflow
on:
  workflow_dispatch:
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Command 0
        run: exit 0
        
      - name: Command 1
        if: success() || failure()
        run: exit 0
        
      - name: Command 2
        if: failure()
        run: echo "failed"
        
        
